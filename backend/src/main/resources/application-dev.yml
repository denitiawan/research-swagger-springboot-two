server:
  port: 5050

spring:

  # mysql
  datasource:
    url: jdbc:mysql://localhost:1234/db_mysql
    username: user
    password: password
    spring:
      datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver



custom:
  # cors
  # mode docker : http://localhost:3001
  # mode dev    : '*'
  app:
    allowedOriginsClientURL: '*'